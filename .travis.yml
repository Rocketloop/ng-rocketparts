language: node_js

node_js:
    - node

stages:
    - test
    - build
    - name: deploy
      if: branch = master

jobs:
    include:
        - script: npm run test:ci
        - stage: build
          script: npm run build
        - stage: deploy
          script: skip
